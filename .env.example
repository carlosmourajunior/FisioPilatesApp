# Configurações do Ambiente - Exemplo
# Copie este arquivo para .env e ajuste os valores conforme necessário

# === CONFIGURAÇÕES DO SERVIDOR ===
# Hostname/IP do servidor (usado para CORS e CSRF)
HOST_DOMAIN=localhost
HOST_IP=127.0.0.1

# Portas dos serviços
FRONTEND_PORT=3000
BACKEND_PORT=8000
NGINX_PORT=80

# === CONFIGURAÇÕES DO DJANGO ===
# Segurança
SECRET_KEY=django-insecure-j*#ciu0i$6cooql@go4_xcx=3bupez6jnpntxtsh94in^2+f=_
DEBUG=True

# === CONFIGURAÇÕES DO BANCO DE DADOS ===
DB_NAME=fisiopilates
DB_USER=fisiouser
DB_PASSWORD=fisiopass
DB_HOST=db
DB_PORT=5432

# === CONFIGURAÇÕES DO REACT ===
# URL da API do backend (usada pelo frontend)
REACT_APP_API_URL=http://localhost:8000

# PWA
REACT_APP_PWA=true

# === CONFIGURAÇÕES DE PRODUÇÃO ===
# Para produção, ajuste os valores abaixo:
# HOST_DOMAIN=seu-dominio.com
# HOST_IP=seu.ip.do.servidor
# REACT_APP_API_URL=https://seu-dominio.com/api
# DEBUG=False
# SECRET_KEY=sua-chave-secreta-super-segura
